# BEETLE Web — 仕様書

最終更新: 2026-05-25

---

## プロジェクト構成

```
beetle-web/
├── index.html              # コーポレートサイト
└── tools/
    └── eigo/
        └── index.html      # えいごよんで！ツール (v2.5)
```

---

## 1. コーポレートサイト (`index.html`)

### 1-1. アクセス制限

- ページ読み込み時に `prompt()` でパスワードを要求
- 正解: `ono`
- 不正解の場合: ページ全体を「アクセスが拒否されました。」メッセージに差し替え
- `sessionStorage` で認証状態を保持（同タブ内はリロード不要、タブを閉じるとリセット）

### 1-2. 心電図アニメーション

- `.ecg-wrap` 内の SVG でスクロールアニメーション
- スクロール速度: `9s linear infinite`（`3s` の3倍遅い）
- 波形: P波・QRS複合波（Q・R・S）・T波 を持つリアルなECGパターン
  - 1拍 = 200px、400px 周期でシームレスループ（4拍分 = 800px SVG）
  - ベースライン y=28、R波ピーク y=5、S波底 y=40、P波ピーク y=22、T波ピーク y=18
- グリッド背景: `<pattern>` で40×14px の薄い格子線（ECG用紙風）
- グロー: `feGaussianBlur` フィルターで発光効果
- 色: `#EF9F27`（アンバー）

---

## 2. えいごよんで！ (`tools/eigo/index.html`)

### 2-1. 概要

小学生向け英語発音練習ツール。単語・英文をスロットに入力し Web Speech API で読み上げる。  
サーバーサイド不要のピュア HTML/CSS/JS（1ファイル完結）。

### 2-2. レスポンシブ対応

| デバイス | 条件 | スロット数 | レイアウト |
|----------|------|-----------|-----------|
| iPhone（モバイル） | `width < 680px` | 3スロット | 1カラム |
| iPad / PC（タブレット以上） | `width ≥ 680px` | 6スロット | 2カラムグリッド（単語モード） |

### 2-3. タブ構成

- **れんしゅう** — メイン練習画面
- **履歴** — 保存済みセット一覧（最大20件、`localStorage`）

### 2-4. 練習モード

| モード | 入力欄 | 説明 |
|--------|--------|------|
| 単語モード | `<input type="text">` | 単語を1つずつ入力 |
| 英文モード | `<textarea rows="2">` | 英文を入力、2行対応 |

### 2-5. 音声機能

#### 読み上げエンジン
- Web Speech API（`SpeechSynthesis`）
- 言語: `en-US`、ピッチ: `1.1`

#### 声の切り替え
- **👧 女の子** / **👦 男の子** ボタンで切り替え
- ボタン押下時の反応音なし
- 女性声の判定: 声の名前に `samantha` / `kate` / `victoria` / `karen` / `moira` / `fiona` / `tessa` / `allison` / `ava` / `susan` / `zoe` / `serena` を含む場合
- デフォルト: 女の子

#### 速度
- **🐢 ゆっくり** = rate 0.6
- **🚶 ふつう** = rate 0.9（デフォルト）
- **🐇 はやく** = rate 1.2
- ボタン押下時の反応音なし

#### 操作
- **まとめて読む** — 入力済みスロットを順番に読み上げ（間隔 600ms）
- **🔊（各スロット）** — 単語を個別に読み上げ
- **とめる** — 読み上げ中断

### 2-6. スロット UI

- 番号バッジ（32px タブレット / 26px モバイル、円形）
- 入力欄（`flex:1`、3px ボーダー）
  - プレースホルダー: 単語モード `えいご１〜えいご６` / 英文モード `えいぶん１〜えいぶん６`（薄いグレー `#ccc`）
- 🔊 ボタン（32px / 26px、バッジと同サイズで左右均等）
- 状態による色変化:
  - アクティブ（読み上げ中）: 紫ボーダー・紫背景 + glow アニメーション
  - 完了: 緑ボーダー・緑背景、バッジが ✓ に
  - 通常: 薄紫ボーダー
- 🔊 ボタンは入力内容がある場合のみ活性化（入力イベントでリアルタイム制御）

### 2-7. 画像OCR機能

#### 概要
写真・画像ファイルから英単語を自動抽出してスロットに取り込む。

#### ライブラリ
- Tesseract.js v4（CDN: `jsdelivr.net/npm/tesseract.js@4/dist/tesseract.min.js`）
- 認識言語: `eng`（英語）

#### 操作フロー
1. 「📷 写真から英単語を取り込む」ボタンをタップ
2. `<input type="file" accept="image/*">` でカメラ撮影またはファイル選択
3. Tesseract.js が OCR 処理（処理中はボタンが「🔍 よみとり中...」に変わり操作不可）
4. 完了後、抽出した単語数ぶんのスロットを表示

#### 単語抽出ルール
- 英字のみで構成（`/^[a-zA-Z]{2,}$/`）かつ2文字以上
- 大文字小文字を区別せず重複除去
- すべて小文字に変換
- 最大20語まで

#### OCRモードのレイアウト
- スロット数: 抽出語数に応じて可変（固定の3/6ではない）
- レイアウト: デバイス問わず1カラム固定
- カード上部に「📷 N語を取り込みました」と「× 通常モードに戻る」を表示

#### リセット
- 🗑️ボタン押下またはモード切替でOCRモード解除 → 通常スロット数（iPhone:3 / iPad・PC:6）に戻る

#### エラー処理
- 英単語が1語も検出されない場合: 「英単語が見つかりませんでした」アラート
- 処理失敗時: 「読み取りに失敗しました」アラート

### 2-8. サンプルデータ

**単語（10語）**

| 単語 | 絵文字 | 日本語 |
|------|--------|--------|
| apple | 🍎 | りんご |
| banana | 🍌 | バナナ |
| cat | 🐱 | ねこ |
| dog | 🐶 | いぬ |
| elephant | 🐘 | ぞう |
| fish | 🐟 | さかな |
| grape | 🍇 | ぶどう |
| house | 🏠 | いえ |
| ice cream | 🍦 | アイスクリーム |
| juice | 🧃 | ジュース |

**英文（6文）**

- I like cats. / This is a pen. / How are you?
- I am happy today. / Let's play together! / Good morning!

タップで空きスロットに自動挿入。

### 2-9. 履歴機能

- 💾 ボタンで現在のスロット内容を保存
- 最大 20 件（超過分は古い順に削除）
- 各エントリ: 単語リスト・モード・保存日時を保存
- 操作: **よみこむ**（練習タブに復元）、**🗑️**（削除）

### 2-10. ヘッダー

- BEETLE合同会社ロゴ（🪲）を表示
- `https://www.beetle-web.jp` へ新タブでリンク
- デザイン: パープル系グラデーション背景にすりガラス風ピル（`backdrop-filter: blur`）

### 2-11. 演出

- 全スロット読み上げ完了後に ⭐ がランダムに飛び出す（8個、0.9s アニメーション）
- 各アニメーション: `starPop` / `pulse`（読み上げ中ボタン） / `glow`（アクティブ入力） / `popIn`（保存メッセージ）

### 2-12. その他

- バージョン表示（`v2.5`）
- 🔄 さいよみこみ ボタン（`location.reload(true)`）
- iPhone ノッチ対応: `env(safe-area-inset-top)`
- テーマカラー: `#764ba2`（パープル）
- リサイズ時にスロット数を自動調整（100ms デバウンス）

---

## 3. 今後の検討事項（未実装）

- [ ] パスワード認証のより堅牢な実装（現状はクライアントサイドのみ）
- [ ] えいごツールのスロット数カスタマイズ
- [ ] 読み上げ回数のカウント・統計表示
- [ ] 音声が使えない環境へのフォールバック表示
