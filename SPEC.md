# BEETLE Web — 仕様書

最終更新: 2026-05-25

---

## プロジェクト構成

```
beetle-web/
├── index.html              # コーポレートサイト
├── about.html
├── beetle_hp_mockup.html
├── README.md
├── SPEC.md
└── tools/
    ├── eigo/
    │   ├── index.html      # えいごよんで！ツール (v2.6)
    │   └── landing.html
    └── bug-checker/
        └── mock.html       # これってバグなの？ モック
```

---

## 1. コーポレートサイト (`index.html`)

### 1-1. アクセス制限

- ページ読み込み時に `prompt()` でパスワードを要求
- 正解: `ono`
- 不正解の場合: ページ全体を「アクセスが拒否されました。」メッセージに差し替え
- `sessionStorage` で認証状態を保持（同タブ内はリロード不要、タブを閉じるとリセット）

### 1-2. 心電図アニメーション

- `.ecg-wrap` 内の SVG でスクロールアニメーション
- スクロール速度: `9s linear infinite`
- 波形: P波・QRS複合波（Q・R・S）・T波 を持つリアルなECGパターン
  - 1拍 = 200px、400px 周期でシームレスループ（4拍分 = 800px SVG）
  - ベースライン y=28、R波ピーク y=5、S波底 y=40、P波ピーク y=22、T波ピーク y=18
- グリッド背景: `<pattern>` で 40×14px の薄い格子線（ECG用紙風）
- グロー: `feGaussianBlur` フィルターで発光効果
- 色: `#EF9F27`（アンバー）

---

## 2. えいごよんで！ (`tools/eigo/index.html`)

### 2-1. 概要

小学生向け英語発音練習ツール。単語・英文をスロットに入力し Web Speech API で読み上げる。  
サーバーサイド不要のピュア HTML/CSS/JS（1ファイル完結）。

### 2-2. レスポンシブ対応

| デバイス | 条件 | スロット数 | レイアウト |
|----------|------|-----------|-----------|
| モバイル | `width < 680px` | 3スロット | 1カラム |
| タブレット以上 | `width ≥ 680px` | 6スロット | 2カラムグリッド（単語モード） |

### 2-3. タブ構成

- **れんしゅう** — メイン練習画面
- **履歴** — 保存済みセット一覧（最大50件、`localStorage` に `eigoHistory` キーで保存）

### 2-4. 練習モード

| モード | 入力欄 | 説明 |
|--------|--------|------|
| 単語モード | `<input type="text">` | 単語を1つずつ入力 |
| 英文モード | `<textarea rows="2">` | 英文を入力、2行対応 |

### 2-5. 音声機能

#### 読み上げエンジン
- Web Speech API（`SpeechSynthesis`）
- 言語: `en-US`、ピッチ: `1.1`

#### 声の選択
- 「🎤 こえ」ボタンで声選択パネルを開閉（▼/▲）
- デバイスで使用可能な英語音声（`lang` が `en` で始まるもの）を2カラムグリッドで一覧表示
- 各音声に **🔊 試すボタン**（"Hello! Nice to meet you!" を再生、選択は変えない）
- 音声名をタップで選択・確定（パネルが閉じる）
- 選択なし（初期状態）: 最初の英語音声を使用

#### 速度

| ボタン | rate | 備考 |
|--------|------|------|
| 🐢 ゆっくり | 0.5 | |
| 🚶 ふつう | 1.0 | デフォルト |
| 🐇 はやい | 1.2 | |

#### 操作
- **まとめて読む** — 入力済みスロットを順番に読み上げ（間隔 600ms）
- **🔊（各スロット）** — 単語を個別に読み上げ
- **とめる** — 読み上げ中断

### 2-6. スロット UI

- 番号バッジ（32px タブレット / 26px モバイル、円形）
- 入力欄（`flex:1 min-width:0`、3px ボーダー）
  - プレースホルダー: 単語モード `えいご１〜６` / 英文モード `えいぶん１〜６`
- 🔊 ボタン（32px / 26px）
- 状態による色変化:
  - アクティブ（読み上げ中）: 紫ボーダー・紫背景 + glow アニメーション
  - 完了: 緑ボーダー・緑背景、バッジが ✓ に
  - 通常: 薄紫ボーダー
- 🔊 ボタンは入力内容がある場合のみ活性化

### 2-7. 画像OCR機能

#### 概要
写真・画像ファイルから英単語を自動抽出してスロットに取り込む。

#### API
- **OCR.space API**（`https://api.ocr.space/parse/image`）
- OCREngine: 2（英語最適化）、scale: true
- APIキー: `OCR_SPACE_KEY` 定数（ファイル冒頭）で管理

#### 操作フロー
1. 「📷 写真から英単語を取り込む」ボタンをタップ
2. `<input type="file" accept="image/*">` を DOM に追加してカメラ/ファイル選択
3. Canvas でリサイズ（最大1500px・JPEG 80%圧縮）→ OCR.space へ送信
4. 処理中はボタンが「🔍 よみとり中...」に変わり操作不可
5. 完了後、抽出した単語数ぶんのスロットを表示

#### 単語抽出ルール
- 英字のみ3文字以上（`/^[a-zA-Z]{3,}$/`）
- ストップワード除外（冠詞・前置詞・助動詞・代名詞・接続詞など約80語）
- 大文字小文字を区別せず重複除去
- すべて小文字に変換
- 最大20語

#### ストップワードの例
`the` / `and` / `is` / `are` / `was` / `have` / `with` / `for` / `this` / `that` / `you` / `they` など

#### OCRモードのレイアウト
- スロット数: 抽出語数に応じて可変
- レイアウト: モバイル含め2カラムグリッド
- 7語以上の場合: スロットエリアに最大高さ260px + スクロール
- カード上部に「📷 N語を取り込みました」と「× 通常モードに戻る」を表示

#### リセット
- 🗑️ボタン押下またはモード切替でOCRモード解除

#### エラー処理
- 英単語が1語も検出されない: 「英単語が見つかりませんでした」アラート
- API エラー: 「読み取りに失敗しました」アラート
- 通信失敗: 「通信に失敗しました」アラート

### 2-8. サンプルデータ

**単語（10語）**

| 単語 | 絵文字 | 日本語 |
|------|--------|--------|
| apple | 🍎 | りんご |
| banana | 🍌 | バナナ |
| cat | 🐱 | ねこ |
| dog | 🐶 | いぬ |
| elephant | 🐘 | ぞう |
| fish | 🐟 | さかな |
| grape | 🍇 | ぶどう |
| house | 🏠 | いえ |
| ice cream | 🍦 | アイスクリーム |
| juice | 🧃 | ジュース |

**英文（6文）**

- I like cats. / This is a pen. / How are you?
- I am happy today. / Let's play together! / Good morning!

タップで空きスロットに自動挿入。

### 2-9. 履歴機能

- 💾 ボタンで現在のスロット内容を保存
- 最大50件（超過分は古い順に削除）
- 各エントリ: 単語リスト・モード・保存日時
- 操作: **よみこむ**（練習タブに復元）、**🗑️**（削除）
- 実装上の注意: グローバル変数名を `historyList` で管理（`window.history` との衝突回避）

### 2-10. ヘッダー

- BEETLE合同会社ロゴ（🪲）を表示
- `https://www.beetle-web.jp` へ新タブでリンク
- デザイン: パープル系グラデーション背景にすりガラス風ピル（`backdrop-filter: blur`）

### 2-11. 演出

- 全スロット読み上げ完了後に ⭐ がランダムに飛び出す（8個、0.9s アニメーション）
- アニメーション: `starPop` / `pulse`（読み上げ中ボタン）/ `glow`（アクティブ入力）/ `popIn`（保存メッセージ）

### 2-12. その他

- バージョン: `v2.6`
- 🔄 さいよみこみ ボタン（`location.reload(true)`）
- iPhone ノッチ対応: `env(safe-area-inset-top)`
- テーマカラー: `#764ba2`（パープル）
- リサイズ時にスロット数を自動調整（100ms デバウンス）

---

## 3. これってバグなの？ (`tools/bug-checker/` または Vercel 独立デプロイ)

最終更新: 2026-05-26

### 3-1. 概要

スクリーンショットをアップロードするだけで AI がバグか否かを判定し、バグの場合は起票内容を自動生成する Web ツール。

- ターゲット：新人テスター・QAエンジニア
- リリース形態：Vercel（Next.js）
- 収益モデル：フリーミアム（グローバル無料枠 + 月額サブスク）
- 参考モック：`tools/bug-checker/mock.html`

### 3-2. 技術スタック

| 役割 | 採用技術 |
|---|---|
| フレームワーク | Next.js 14（App Router） |
| 言語 | TypeScript |
| スタイル | Tailwind CSS |
| ホスティング | Vercel |
| KV | Vercel KV（Redis） |
| 課金 | Stripe（サブスク） |
| AI | Claude Haiku API（`claude-haiku-4-5-20251001`） |
| アイコン | Tabler Icons（`@tabler/icons-react`） |

### 3-3. 料金プラン

| プラン | 料金 | 制限 |
|---|---|---|
| 無料 | 無料 | グローバル月10,000回まで（全ユーザー共通枠） |
| サブスク | 月額480円 | 月240回 |

- カウンターは Vercel KV にグローバル1個で管理（キー: `scan:count:YYYY-MM`）
- 上限値は環境変数 `FREE_LIMIT` で管理（デフォルト 10,000）
- 毎月1日 0:00（JST）に自動リセット
- 上限到達時はサブスク登録ページへ誘導

### 3-4. 画面構成

```
/ （メイン）
  ├── ヘッダー：アプリ名 + グローバルカウンター
  ├── 画像アップロードエリア
  ├── スキャン実行ボタン
  ├── スキャンアニメーション（判定中）
  ├── 判定結果 + 起票カード（判定後）
  └── フッター：リリース日

/subscribe  → Stripe チェックアウトへリダイレクト
/success    → 支払い完了ページ
/cancel     → キャンセルページ
```

### 3-5. UIコピー

| 要素 | テキスト |
|---|---|
| アプリ名 | これってバグなの？ |
| バッジ | BUG DETECTOR |
| サブタイトル | 画像を貼るだけで判定 → バグなら起票内容を自動生成 |
| カウンター | 今月みんなで {used} 回使いました（無料枠 {limit} 回まで） |
| スキャンボタン | バグスキャン実行 |
| バグ判定 | 残念... バグです。 |
| 非バグ判定 | バグではなさそうです |
| 不明判定 | 判断が難しいです |
| 無料枠終了 | 今月の無料枠が終了しました。サブスク登録（月額400円）で引き続きご利用いただけます。 |
| フッター | リリース日：{NEXT_PUBLIC_RELEASE_DATE} |

### 3-6. スキャンアニメーション仕様

ボタン押下後の画面遷移：

**① スキャン中（約3秒）**

```
ステップ1（0〜900ms）    スピナー → チェック「画像を解析中...」
ステップ2（900〜1800ms） スピナー → チェック「UIパターンを確認中...」
ステップ3（1800〜2700ms）スピナー → チェック「バグ判定中...」
```

**② 判定結果の表示（2700ms以降）**

スキャンエリアが消えて判定カードがポップインで登場。
バグの場合は 0.2秒後に起票カードがスライドダウンで表示。

| アニメーション | 対象 | 仕様 |
|---|---|---|
| verdictPop | 判定カード | scale(0.92)→scale(1.03)→scale(1), 0.5s, cubic-bezier(0.34,1.56,0.64,1) |
| slideDown | 起票カード | translateY(-10px)→0, opacity 0→1, 0.4s ease, delay 0.2s |
| spin | スピナー | rotate 360deg, 0.7s linear infinite |

### 3-7. API設計

#### POST /api/scan

**リクエスト**
```json
{
  "image": "base64文字列",
  "mimeType": "image/png",
  "subscriptionToken": "トークン（任意）"
}
```

**レスポンス（成功）**
```json
{
  "verdict": "bug" | "not_bug" | "unclear",
  "reason": "判定理由（日本語）",
  "ticket": {
    "title": "バグタイトル",
    "severity": "Critical | High | Medium | Low",
    "category": "UI | API | Performance | Logic | Security",
    "description": "詳細説明",
    "steps": ["手順1", "手順2", "手順3"],
    "expected": "期待する動作",
    "actual": "実際の動作",
    "env": "環境情報"
  } | null,
  "remaining": 6153
}
```

**レスポンス（無料枠超過）**
```json
{ "error": "FREE_LIMIT_EXCEEDED", "remaining": 0 }
```

**処理フロー**
1. `subscriptionToken` を Vercel KV で確認 → 有効ならカウントせず処理
2. 今月カウンターを取得
3. `FREE_LIMIT` 以上なら `FREE_LIMIT_EXCEEDED` を返す
4. Claude Haiku API に画像を送信して判定
5. カウンターをインクリメント
6. 結果を返す

**Claude Haiku システムプロンプト**
```
あなたはソフトウェアQAの専門家です。アップロードされた画像を見て、バグ・不具合かどうかを判断してください。
以下のJSON形式のみで回答してください。他のテキストは一切含めないでください。
verdict が "not_bug" または "unclear" の場合、ticket は null にしてください。
```

#### GET /api/count
```json
{ "used": 3847, "limit": 10000, "remaining": 6153, "resetAt": "2026-06-01T00:00:00+09:00" }
```

#### POST /api/checkout
Stripe サブスクチェックアウトセッションを作成。メールを受け取り URL を返す。

#### POST /api/webhook
- `checkout.session.completed` → トークン発行・メール送信
- `customer.subscription.deleted` → トークン削除

### 3-8. Vercel KV データ構造

```
scan:count:{YYYY-MM}   → number
subscription:{token}   → { email, stripeCustomerId, status, createdAt }
```

### 3-9. 環境変数

```env
ANTHROPIC_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
FREE_LIMIT=10000
NEXT_PUBLIC_RELEASE_DATE=2026-06-01
NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
```

### 3-10. 非機能要件

- 画像サイズ上限：5MB（フロント側でバリデーション）
- 対応フォーマット：PNG / JPEG / WebP / GIF
- API タイムアウト：30秒
- レート制限：同一 IP から1分間に10リクエストまで（Vercel Edge Middleware）

---

## 4. 今後の検討事項（未実装）

- [ ] パスワード認証のより堅牢な実装（現状はクライアントサイドのみ）
- [ ] スロット数カスタマイズ
- [ ] 読み上げ回数のカウント・統計表示
- [ ] 音声が使えない環境へのフォールバック表示
- [ ] OCR.space 月間上限（25,000回）を超えた場合の対応
- [ ] バグ判定：Jira / Linear への直接起票連携
- [ ] バグ判定：サブスク会員向けの判定履歴保存機能
- [ ] **Next.js を 16 系へメジャー更新（gakkyu-tsushin / bug-checker 両方）**
  - 現状: 両ツールとも `next@14.2.35`（2025-12-11 告知の脆弱性はパッチ済み）で運用中
  - `npm audit` は過去の Next 脆弱性をまとめて表示する仕様のため、なお「2 vulnerabilities」が残る。これを完全に解消するには `next@16` へのメジャー更新が必要（audit の自動提案も next@16）
  - 14 → 15 → 16 は breaking change あり（App Router の request API が async 化 等）。動作確認が必要なので、まとまった時間が取れるタイミングで別ブランチで実施する
  - 優先度: 中（告知済みの脆弱性は対応済みのため緊急ではない。GitHub Pages / Vercel 用途では実害リスクも小さい）